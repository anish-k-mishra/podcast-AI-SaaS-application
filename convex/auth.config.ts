// https://comic-mouse-17.clerk.accounts.dev

const authConfig = {
    providers: [
      {
        domain: "https://comic-mouse-17.clerk.accounts.dev",
        applicationID: "convex",
      },
    ]
  };

  export default authConfig;